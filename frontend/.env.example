# ===== 演示模式开关 =====
# true  ：演示模式，无 token 自动注入 demo-token（跳过登录），适合本地演示
# false ：生产模式，强制真实登录，需配合后端 AUTH_REQUIRED=true
# 未设置：默认演示模式（兼容旧行为）
VITE_DEMO_MODE=true

# ===== 附加模块开关（可插拔）=====
# 语音克隆（客服音色）。默认 false（未设置即为关），与后端 VOICE_CLONE_ENABLED 一一对应。
# false：设置页的「客服语音」Tab 整个不渲染（不是显示空态）+ 后端路由不注册。
# true ：需同时把 backend/.env 的 VOICE_CLONE_ENABLED 设为 true，
#        并配好 PUBLIC_BASE_URL（CosyVoice 要求音频样本公网可回源）。
VITE_VOICE_CLONE_ENABLED=false
