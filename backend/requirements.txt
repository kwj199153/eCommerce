# ====== 跨境电商 AI SaaS - 后端依赖 ======
# 安装：pip install -r requirements.txt
# 说明：本清单以「代码里真实 import 的包」为准，已剔除历史遗留的未使用依赖。

# ---------- Web 框架 ----------
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.0
python-multipart>=0.0.6

# ---------- LLM & Agent ----------
httpx>=0.25.0          # DashScope / SP-API HTTP 调用
langchain-core         # LangChain 核心
langgraph              # LangGraph 工作流引擎

# ---------- RAG 向量检索 ----------
numpy>=1.26.0          # 向量计算
scikit-learn>=1.3.0    # TF-IDF / 余弦相似度
jieba>=0.42.1          # 中文分词

# ---------- 认证 & 安全 ----------
python-jose[cryptography]>=3.3.0  # JWT 编解码
bcrypt>=4.1            # 密码哈希（直接调用 bcrypt，不经 passlib）
# 注意：不再依赖 passlib。passlib 1.7.4 读取 bcrypt>=4.1 已移除的
#       bcrypt.__about__.__version__ 会导致注册/登录 500。

# ---------- 数据库 ----------
sqlalchemy[asyncio]>=2.0   # ORM（[asyncio] 带 greenlet，异步必需）
asyncpg>=0.29              # 业务运行时 PG 驱动（postgresql+asyncpg）
psycopg2-binary>=2.9       # Alembic 迁移专用同步驱动（postgresql+psycopg2）
alembic>=1.12              # 数据库迁移
python-dotenv>=1.0.0       # .env 加载（alembic/env.py）

# ---------- 缓存 & 异步任务 ----------
redis>=5.0.0           # Redis 客户端（缓存 / 限流）
celery>=5.3            # 分布式任务队列

# ---------- 日志 ----------
loguru>=0.7            # 结构化日志

# ---------- 测试 ----------
pytest>=7.4            # 测试框架
pytest-asyncio>=0.23   # 异步测试支持
