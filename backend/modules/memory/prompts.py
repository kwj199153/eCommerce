"""长期记忆的**抽取提示词**（业务层，第 151 轮从 `ai_infra` 搬来）。

★ 为什么它在 `modules/` 而不在 `ai_infra/`
`ai_infra` 的字符串层有硬门禁：**零业务内容**
（`tests/test_infra_layering.py::test_ai_infra_string_literals_have_no_business_content`）。
而提示词就是业务语义本身 —— 它决定「什么值得记住」、拿什么当例子。
第 151 轮实测它被门禁咬中：提示词里写着「竞品价格 / 某个 ASIN 的参数 /
某天的销量 / 广告 ACOS」，那是跨境电商的判断，不是通用机制。

⇒ 机制留在 `ai_infra/memory/distill.py`（转录渲染 / 拼装 / JSON 抠取 / 去重 / 上限），
  提示词住这里，由 `build_extract_prompt(..., instructions=EXTRACT_INSTRUCTIONS)` 注入。
⇒ 本模块是它的**唯一真源**：`ai_infra` 那边**没有**默认值，只有一个必填参数。

★ 改这段提示词时的判据
它不是文案，是**行为规格**：改一个「不要提取」的类别，等于改「记忆里会出现什么」。
⇒ 改完必须真跑一次抽取（拿一段真对话），看候选是不是你预期的那些，
  而不是跑一遍单测就收工（单测喂的是桩，桩不会告诉你提示词变差了）。
"""

#: 抽取提示词的固定开头（**业务判断**：什么值得记住、拿什么当例子）。
#:
#: ★ 刻意把"不要提取什么"写全：模型在这个任务上最大的失效模式不是漏抽，
#:   而是**把一次性任务当成长期偏好**（"帮我把标题改短"→"用户偏好短标题"）。
#:   一条错误的长期记忆比少一条记忆有害得多：它会**永久**影响之后每一轮回答，
#:   而用户在界面上看到的只是一条看着挺像那么回事的句子。
EXTRACT_INSTRUCTIONS = """你是长期记忆整理助手。下面是用户与 AI 助手最近的对话片段。

只提取**关于这位用户本人、且在未来对话中仍然成立**的事实，例如：他的身份与
业务背景、常用工具、运营与投放偏好、沟通风格、决策习惯、当前关注重点。

**不要**提取以下四类：
  1. 一次性的任务内容（"帮我把这个标题改短" ≠ "用户偏好短标题"）；
  2. AI 自己给出的结论或建议（那是 AI 说的，不是用户的偏好）；
  3. 对话里出现的第三方数据（竞品价格、某个 ASIN 的参数、某天的销量）；
  4. 已经写在下方【已有记忆】里的内容。

分节只能取以下之一：工作背景、个人背景、运营偏好、沟通偏好、关注重点

输出**严格 JSON 数组**，不要解释文字、不要 markdown 代码块：
[{"section": "运营偏好", "content": "广告 ACOS 控制在 25% 以内算健康，新品期容忍 40% 以内"}]

没有值得记住的内容就输出 []
"""
